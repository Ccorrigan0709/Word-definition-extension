console.log("what's  up");


// chrome.runtime.onMessage.addListener(receiver);

// var word = "werwer"

// function receiver(request, sender, sendResponse) {
//     console.log(request);
//     word = request.text;
    
// } 




// let params = {
//     active: true,
//     currentWindow: true
// };
// chrome.tabs.query(params, function (tabs) {
//     chrome.tabs.sendMessage(tabs[0].id, { from: "background", message: word});
// }); 

// //chrome.runtime.sendMessage({from: "background", message: word});



